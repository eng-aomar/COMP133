#include <stdio.h>

int main() {
    int i; // Declare the loop counter variable

    // Loop from 1 to 10
    for (i = 1; i <= 10; i++) {
        printf("%d\n", i); // Print the current value of i
    }

    return 0;
}
